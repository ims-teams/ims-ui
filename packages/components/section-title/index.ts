import { withInstall } from "@ims-ui/utils";
import SectionTitle from "./src/index.vue";

const ImsSectionTitle = withInstall(SectionTitle);

export { ImsSectionTitle };

export default ImsSectionTitle;
